# Deno Hooks Configuration
# Learn more: https://jsr.io/@theswanfactory/deno-hooks

hooks:
  pre-commit:
    - deno task fmt
    - deno task lint

  pre-push:
    - deno task test
    - deno run -A scripts/test-fake-repo.ts
