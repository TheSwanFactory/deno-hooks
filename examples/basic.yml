# Basic deno-hooks configuration
# This example shows the simplest setup

hooks:
  pre-commit:
    # Format code with deno fmt
    - deno task fmt

    # Lint code with deno lint
    - deno task lint

  pre-push:
    # Run tests before pushing
    - deno task test
